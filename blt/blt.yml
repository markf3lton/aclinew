project:
  human_name: drupal9
  machine_name: drupal9
  local:
    hostname: local.drupal9.com
    protocol: http
    uri: '${project.local.protocol}://${project.local.hostname}'
  prefix: BLT
  profile:
    name: standard
cm:
  strategy: config-split
git:
 default_branch: develop
 remotes:
  - eemfelton@svn-14671.prod.hosting.acquia.com:eemfelton.git

